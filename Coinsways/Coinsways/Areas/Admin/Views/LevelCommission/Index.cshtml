@model IEnumerable<Coinsways.Entities.LevelCommission>

@{
    ViewBag.Title = "Level Commission::";
}
<div class="content">
    <div class="container-fluid">
        <div class="row">
            <div class="col-md-12">
                <div class="card">
                    <div class="card-header" data-background-color="purple">
                        <h4 class="title">Level Commission</h4>
                        <p>
                            @Html.ActionLink("Create New", "Create", null, new { @class = "btn-danger", @style = "padding: 5px; border-radius: 4px;" })
                        </p>
                    </div>
                    <div class="card-content table-responsive">
                        <table class="display responsive no-wrap" style="width: 100%;" id="lvlComTbl">
                            <thead>
                                <tr>
                                    <th>
                                        @Html.DisplayNameFor(model => model.LevelDetails)
                                    </th>
                                    <th>
                                        @Html.DisplayNameFor(model => model.Percentage)
                                    </th>
                                    <th>
                                        @Html.DisplayNameFor(model => model.IsActive)
                                    </th>
                                    <th></th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var item in Model)
                                {
                                    <tr>
                                        <td>
                                            @Html.DisplayFor(modelItem => item.LevelDetails)
                                        </td>
                                        <td>
                                            @Html.DisplayFor(modelItem => item.Percentage)
                                        </td>
                                        <td>
                                            @Html.DisplayFor(modelItem => item.IsActive)
                                        </td>
                                        <td>
                                            @Html.ActionLink("Edit", "Edit", new { id = item.Id }) |
                                            @Html.ActionLink("Details", "Details", new { id = item.Id }) |
                                            @Html.ActionLink("Delete", "Delete", new { id = item.Id })
                                        </td>
                                    </tr>
                                }
                            </tbody>
                        </table>

                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<script>
    $("#lvlComTbl").dataTable({
        responsive: true,
        "bLengthChange": false,
        "pageLength": 10,
        "aoColumnDefs": [
            { 'bSortable': false, 'aTargets': [3] }
        ]

    });
</script>
